import type { Metadata } from "next";
import Link from "next/link";
import { ArrowUpRight } from "lucide-react";

export const metadata: Metadata = { title: "更新记录", description: "Paistar 工具台的公开更新记录。" };
const updates = [{ version: "2.0", date: "2026.09.24", title: "从手记到工具台", notes: ["重新设计首页与移动端导航，加入透明玻璃材质和深浅色主题。", "上线 JSON、时间戳、Base64/URL、文本对比、二维码五个纯前端工具。", "新增全局搜索、分类筛选与独立工具详情页。"] }, { version: "1.0", date: "2026.09.24", title: "第一版静态站", notes: ["使用 Astro 创建个人手记，用于验证 GitHub Pages 与自定义域名链路。", "旧版本已标记为 blog-v1，可以从 GitHub 标签回退。"] }];
export default function UpdatesPage() { return <div className="container-wide pt-17 md:pt-24"><p className="eyebrow text-primary">CHANGELOG / OPEN NOTES</p><h1 className="display-heading mt-6">每一次，<br/><span className="text-[#6d62d7] dark:text-[#afa6ff]">都再顺手一点。</span></h1><p className="mt-7 max-w-xl text-[15px] leading-8 text-muted-foreground">新工具、细节调整和设计变化会记录在这里。你也可以在 GitHub 查看每次提交。</p><div className="mt-16 border-t border-border">{updates.map((update) => <article key={update.version} className="grid gap-5 border-b border-border py-10 md:grid-cols-[220px_1fr]"><div><span className="eyebrow text-primary">VERSION {update.version}</span><p className="mt-2 font-mono text-[12px] text-muted-foreground">{update.date}</p></div><div><h2 className="text-[26px] font-bold tracking-[-.05em]">{update.title}</h2><ul className="mt-5 space-y-3 text-[14px] leading-7 text-muted-foreground">{update.notes.map((note) => <li key={note} className="flex gap-3"><span className="mt-3 h-1.5 w-1.5 shrink-0 rounded-full bg-primary"/>{note}</li>)}</ul></div></article>)}</div><Link href="/#find" className="mt-10 inline-flex items-center gap-2 text-[13px] font-semibold text-primary">回到工具台 <ArrowUpRight size={16}/></Link></div>; }
